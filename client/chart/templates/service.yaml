apiVersion: v1
kind: Service
metadata:
  annotations:
    prometheus.io/scrape: 'true'
  name: "frontend-service"
spec:
  ports:
  - name: http
    port: {{ .Values.service.servicePort }}
    nodePort: 3000
  type: NodePort
  selector:
    app: "frontend-selector"
