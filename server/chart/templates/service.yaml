apiVersion: v1
kind: Service
metadata:
  annotations:
    prometheus.io/scrape: 'true'
  name: "backend-service"
spec:
  ports:
  - name: http
    port: {{ .Values.service.servicePort }}
    nodePort: 5000
  type: NodePort
  selector:
    app: "backend-selector"
